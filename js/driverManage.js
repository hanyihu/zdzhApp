$(function (){
    var myScroll;
    myScroll = new IScroll('#wrapper', { scrollX: true, freeScroll: true });
    $('#information-right').smint();


// document.addEventListener('touchmove', function (e) { e.preventDefault(); }, isPassive() ? {
// 	capture: false,
// 	passive: false
// } : false);

})